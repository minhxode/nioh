interface GlobalFlags {
    _token?: string;
}
declare class GlobalFlags implements GlobalFlags {
    get token(): string | undefined;
    set token(token: string | undefined);
}
declare const globalFlags: GlobalFlags;
export default globalFlags;
