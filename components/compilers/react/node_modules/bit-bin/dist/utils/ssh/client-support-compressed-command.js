"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = clientSupportCompressedCommand;

function _semver() {
  const data = _interopRequireDefault(require("semver"));

  _semver = function () {
    return data;
  };

  return data;
}

function clientSupportCompressedCommand(clientVersion) {
  // The compress support released in version 14.6.0, older version doesn't support it
  return clientVersion && _semver().default.gte(clientVersion, '14.6.0');
}